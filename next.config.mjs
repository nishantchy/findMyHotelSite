/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["https://findmyhotelbackend-production.up.railway.app"],
  },
};

export default nextConfig;
